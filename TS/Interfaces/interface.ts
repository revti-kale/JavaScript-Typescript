interface abc {
    name:string;
    age:number;
    address:string;
}

const person:abc={
    name:'revti',
    age:23,
    address: 'pune',
};

console.log(person.name);
console.log(person.age);
console.log(person.address)